因为多个写线程同时写 table 时，存在同步问题，put 时加锁即可解决。
但是不能直接一个锁，如果对所有 table 共用一个锁，那么 put 实际上单线程没差，甚至多线程比单线程还要慢，这是因为每一次只能有一个线程在写 table，
并在多线程等待 lock 还会花时间，因此比单线程还慢。
解决办法就是常规的降低锁粒度。
因为每个 bucket（==5） 之间是互不影响的，因此对每个 bucket 一个独立的锁即可。
即使这样，也只能在 thread_num <= 5 时加速，因为同一时间最多 5 个线程在写，当超过 5 时多余的线程会被阻塞。